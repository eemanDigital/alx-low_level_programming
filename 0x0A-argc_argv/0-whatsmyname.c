#include <stdlib.h>
#include <stdio.h>
/**
 * main- a program that prints its name, followed by a new line.
 * @argc: store the number of arguments passed
 * @argv: array pointer that holds the arguments passed
 * Return: 0
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
