#include "main.h"
/**
 *_abs - entry point
 * Description: Print absolute value of integer
 *
 * @n: check function input
 *
 * Return: returns outputs
 */
int _abs(int n)
{
	if (n > 0)
	{
	return (n);
	}
	else if (n < 0)
	{
	return (-n);
	}
	return (n);

}
