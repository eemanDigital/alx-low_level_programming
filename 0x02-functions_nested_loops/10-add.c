#include "main.h"
/**
 * add - sum to integers
 *
 * @x: first number
 * @y: second number
 *
 * Return: the addition of numbers
 */
int add(int x, int y)
{
	int total;

	total = x + y;
	return (total);
}
