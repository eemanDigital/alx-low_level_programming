#include "main.h"
/**
 * _strlen - function that returns the length of a string.
 * @s: point variable
 * Return: the legnth of the string
 */
int _strlen(char *s)
{
	int l = 0;

	if (s[l])
	l++;
	return (l);
}
